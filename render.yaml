services:
  - type: static
    name: romantic-hacker
    buildCommand: null  # No build step needed for simple static files
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
